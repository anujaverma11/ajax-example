# rake -T to see tasks in Terminal

# require ALL files
Dir.glob(File.join('**','*.rb')).each { |file| require './' + file }

# test...
desc 'test rake task'
task :trajan do
  puts 'Trajan was a notable Roman emperor'
end
